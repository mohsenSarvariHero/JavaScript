console.log("hello")

const numbers = [];
const show = (x) => {
    numbers.push(x)
    numbers.forEach((item)=>{
    console.log(item)
    })
}
const result = (x) => {
    numbers.forEach((item)=>{
        if(item == '+')
            num
    let num1 = item
    })
}
